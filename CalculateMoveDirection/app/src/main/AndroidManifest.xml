<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iboxshare.testgyroscope">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/shoubing"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--SSPLibrary-->
        <activity android:name="app.akexorcist.bluetotohspp.library.DeviceList" />




        <!--SplashActivity-->
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--MainActivity-->
        <activity android:name=".ui.MainActivity"/>


        <!--MotionSensoringModeActivity-->
        <activity android:name=".ui.MotionSensoringModeActivity"
            android:screenOrientation="locked"/>

        <!--ButtonModeActivity-->
        <activity android:name=".ui.ButtonModeActivity"/>

    </application>

</manifest>